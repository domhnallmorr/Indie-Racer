# Painting a livery

`template.svg` is a 2048 x 2048 UV wire template. Each named SVG group corresponds to one painted mesh (Nose, SidepodLeft, SidepodRight, EngineCover, wings, endplates, etc.). `layout.json` lists those names, paint materials and UV bounding boxes. The two sides are separate, not mirrored. All panels use the same atlas.

Open the SVG in a painting/vector editor, keep its wire groups on top as guides, and paint on layers underneath. Hide the wire guides before exporting the finished 2048 x 2048 PNG. Add at least 8 pixels of colour bleed around the islands to avoid mipmap seams. Blender's UV Editing workspace can show the selected part against the same atlas. `base_white.png` is the neutral default texture, not a finished colour scheme.

To apply artwork later, call this after the car's default roster colour has been assigned:

```gdscript
var skins = preload("res://content/vehicles/open_wheel/liveries/apply_livery.gd")
skins.apply(vehicle.get_node("Visual"), load("res://path/to/finished_skin.png"))
```

This duplicates the two paint materials per instance, installs the same image in both, and resets their tint to white so the artwork retains its colours. Rubber, mechanical parts and driver helmet are unaffected. Roster entries can now supply an optional `livery` content texture path, validated before spawning and applied after the roster tint.

## 2001 IRL roster

Select **2001 IRL** in F12 and restart practice to use the growing season roster, currently Scott Sharpe (#8 Delphi) Sam Hornish Jnr. (#4 Panther Racing / Pennzoil), Buddy Lazier (#91 Hemelgarn Racing), Billy Boat (#98 CURB Records), Eliseo Salazar (#14 Harrah's), Felipe Giaffone (#21 Hollywood), Al Unser Jr. (#3 Starz), Eddie Cheever (#51 Excite), Buzz Calkins (#12 Bradley), Airton Daré (#88 Bar None), Jeff Ward (#35 Aerosmith / Menards), Robbie Buhl (#24 Purex), Shigeaki Hattori (#55 Epson), Mark Dismore (#28 Delphi), Donnie Beechler (#84 Harrah's), and Robbie McGehee (#10). It contains sixteen AI entries plus the player; the original test rosters are still available.

Sam's `sam_hornish_2001.png` uses golden yellow paint, outlined Pennzoil lettering, a Chevrolet bowtie, Rexhall endplate badges, a simplified panther outline and nose number 4, reconstructed from the supplied side photograph. Unseen surfaces are inferred; the existing chassis and default helmet remain. Ratings are benchmark values rather than historical performance data.

Build it with `python tools/build_hornish_livery.py`, which shares `tools/livery_baker.py` with Scott's artwork generator. After texture reimport, `tools/capture_car_2001.gd -- --hornish` produces four runtime views; `tools/validate_irl_2001.gd` checks both spawned cars and their independent textures.

### Earlier Delphi test field

In F12, select **Scott Sharpe livery test â€” Delphi 2001** and restart practice. This separate 15-car roster uses the Grant slot for Scott Sharpe, number 8; all existing rosters remain available. Ratings are inherited benchmark values, not historical performance claims.

`scott_sharpe_2001.png` recreates the supplied photograph's red sidepods, yellow borders/endplates, black upper body, Delphi/Futaba lettering and number 8 on the existing IR-05 mesh. Sponsor lettering and small badges are approximations; the current chassis and default helmet remain. The unseen side is inferred from the visible side.

Rebuild with `python tools/build_sharpe_livery.py` (Windows, Pillow, numpy and System.Drawing). The baker projects artwork in body coordinates into the GLB's existing UV islands, adds gutter bleed, and leaves the mesh untouched. Reimport the texture, then run `tools/capture_car_2001.gd -- --sharpe` for four runtime views or `tools/validate_sharpe.gd` for roster/spawn checks.

The mesh generator packs the atlas; geometry changes and rebuilds may repack it. Finish body edits before producing final artwork, and version the GLB, template and skin together. A diagnostic checker render is generated by `tools/capture_car_2001.gd` in `builds/car_2001_uv_check.png`.

### Buddy Lazier / Hemelgarn #91

`buddy_lazier_2001.png` recreates the supplied reference with deep purple lower panels and nose, white upper body and wings, a fluorescent yellow shoulder stripe, TAEBO sidepods, Coors Light and Life Fitness branding, and number 91. Lettering and small sponsors are approximations adapted to the current chassis. Rebuild using `python tools/build_lazier_livery.py`; capture with `tools/capture_car_2001.gd -- --lazier`. The 2001 IRL validator checks all roster cars.

### Billy Boat / CURB Records #98

`billy_boat_2001.png` follows the supplied photograph with blue sidepods, nose and wings, a pearl-white upper body, red pinstripes, slanted CURB Records lettering, an Infiniti badge, and white 98 numbers. Small sponsor marks are approximations on the existing chassis. Rebuild with `python tools/build_boat_livery.py`; render four views using `tools/capture_car_2001.gd -- --boat`.

### Eliseo Salazar / Harrah's #14

`eliseo_salazar_2001.png` recreates the reference's purple sidepods and wings, white nose and upper body, yellow accents, white star trail, Harrah's wordmarks, Cristal badges and black 14 number panels. Fine sponsor details are approximations on the existing chassis. Rebuild with `python tools/build_salazar_livery.py`; capture with `tools/capture_car_2001.gd -- --salazar`.

### Felipe Giaffone / Hollywood #21

`felipe_giaffone_2001.png` follows the supplied reference: navy Hollywood sidepods and fin, fluorescent coral-red rear panels, green nose with jagged red/navy borders, white 21 numbers and Firehawk lettering. Sponsor details and unseen surfaces are approximations on the existing chassis. Rebuild with `python tools/build_giaffone_livery.py`; capture with `tools/capture_car_2001.gd -- --giaffone`.

### Al Unser Jr. / Starz #3

`al_unser_jr_2001.png` recreates the supplied white Starz Super Pak. car with a black nose and engine-cover cap, fine red shoulder trim, Budweiser script and black 3 panels. Sponsor lettering and small marks are approximations on the existing chassis. Rebuild with `python tools/build_unser_livery.py`; render with `tools/capture_car_2001.gd -- --unser`.

### Eddie Cheever / Excite #51

`eddie_cheever_2001.png` follows the supplied silver-and-black reference with red pinstripes, Excite lettering and radiating-X graphics, Infiniti badges, @Home shoulder branding and 51 panels. Small sponsor details are approximations on the existing chassis. Rebuild with `python tools/build_cheever_livery.py`; render using `tools/capture_car_2001.gd -- --cheever`.

### Buzz Calkins / Bradley #12

`buzz_calkins_2001.png` recreates the supplied all-red reference with white Bradley sidepod lettering, Sav-O-Mat script, Sinclair branding, black 12 numbers in white panels and a yellow contingency badge. Small sponsor details are approximations on the existing chassis. Rebuild with `python tools/build_calkins_livery.py`; capture using `tools/capture_car_2001.gd -- --calkins`.

### Airton Daré / Bar None #88

`airton_dare_2001.png` follows the supplied fluorescent yellow-green and deep purple reference, with staggered 1-800-BAR NONE lettering, black 88 numbers in white panels, Infiniti badges and Firehawk wing branding. Small sponsor details are approximations on the existing chassis. Rebuild with `python tools/build_dare_livery.py`; capture with `tools/capture_car_2001.gd -- --dare`.

### Jeff Ward / Aerosmith #35

`jeff_ward_2001.png` follows the supplied pink-and-silver reference with a torn paint boundary, reconstructed winged Aerosmith emblem, Menards nose panels, Pennzoil badge and number 35. The blue figure and yellow cloth on the engine cover are a simplified vector-style interpretation, not an exact reproduction. Rebuild with `python tools/build_ward_livery.py`; render with `tools/capture_car_2001.gd -- --ward`.

### Robbie Buhl / Purex #24

`robbie_buhl_2001.png` recreates the supplied blue, yellow and orange-red Purex reference, including a white pinstripe, outlined Purex emblem, Infiniti badge, Racing for Kids/Aventis lettering and large blue 24 rear-endplate numbers. Sponsor artwork is an approximation on the existing chassis. Rebuild with `python tools/build_buhl_livery.py`; capture with `tools/capture_car_2001.gd -- --buhl`.

### Shigeaki Hattori / Epson #55

`shigeaki_hattori_2001.png` follows the supplied blue-and-white reference with large Epson sidepod lettering, a sweeping white nose stripe, Ibaraki Toyopet and Bell Mare lettering, and number 55. Small sponsor marks are approximations on the existing chassis. Rebuild with `python tools/build_hattori_livery.py`; capture with `tools/capture_car_2001.gd -- --hattori`.

### Mark Dismore / Delphi #28

`mark_dismore_2001.png` shares Scott Sharpe’s Delphi paint and sponsor layout, with 28 numbers and DISMORE lettering. Rebuild using `python tools/build_sharpe_livery.py --dismore`; capture using `tools/capture_car_2001.gd -- --dismore`. Running the generator without the flag still produces Scott’s original #8.

### Donnie Beechler / Harrah's #84

`donnie_beechler_2001.png` follows the supplied purple-and-white reference with a yellow intake/fin, Harrah's wordmarks, black 84 numbers and a reconstructed red sponsor panel. Small lettering is approximate given the reference resolution. Rebuild with `python tools/build_beechler_livery.py`; capture with `tools/capture_car_2001.gd -- --beechler`. The track now provides 26 race pit boxes, including the player, plus a separate pace-car station.

### Robbie McGehee #10

`robbie_mcgehee_2001.png` follows the supplied small reference with near-black bodywork, blue lower panels, silver rear wing and white 10 numbers. Sponsor details are approximate given the reference resolution. Rebuild with `python tools/build_mcgehee_livery.py`; render with `tools/capture_car_2001.gd -- --mcgehee`. The sixteen-driver roster fits the existing expanded pit layout.
